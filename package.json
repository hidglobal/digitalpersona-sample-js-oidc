{
  "version": "1.0.0",
  "name": "digitalpersona.sample.js.oidc",
  "private": true,
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {},
  "dependencies": {
    "express": "^4.17.0",
    "eslint": "^5.16.0"
  },
  "eslintConfig": {
    "env": {
      "browser": true,
      "node": true
    },
    "rules": {
      "semi": "error"
    }
  }
}
